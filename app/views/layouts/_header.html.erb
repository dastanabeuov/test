<div class="nav">
  <span class="brand"><%= link_to 'TestGuru', root_path %></span>
  <div class="right">
    <% if user_signed_in?%>
      <%= t('.greetings', email: current_user.email) %> | 
      <b><%= link_to t('common.logout'), destroy_user_session_path, method: :delete %></b>
    <% else %>
      <%= link_to t('common.login'), new_user_session_path%> |
      <%= link_to t('common.signup'), new_user_registration_path%>
    <% end %>
  </div>
</div>