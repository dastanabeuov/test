<div class="nav">
  <span class="brand"><%= link_to 'TestGuru', root_path %></span>
  <div class="right">
    <% if logged_in? %>
      <h1>Welcome, <b><%= current_user.email %></b> Guru</h1>
      <%= link_to 'Logout', sessions_path, method: :delete %>
    <% else %>
      <%= link_to 'Login', login_path %>
      <%= link_to 'SignUp', signup_path %>
    <% end %>
  </div>
</div>